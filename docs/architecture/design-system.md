# Pragion Design System

Pragion UI owns tokens, typography, colors, spacing, surfaces, controls, interaction states, responsive behavior, themes, and accessibility conventions. The system intentionally does not depend on Bootstrap or another CSS framework.

Tokens live in `frontend/styles/tokens.css`. Components use those variables instead of scattering arbitrary values. Themes override the same variables through `data-pragion-theme`.
