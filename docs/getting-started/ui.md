# Running the UI Gallery

From the repository root:

```bash
pragion ui gallery
```

The command serves local frontend assets at `http://127.0.0.1:8765/pages/ui-gallery.html` and opens the default browser. It resolves assets from the project checkout or from the installed wheel. Stop it with `Ctrl+C`. No internet or CDN is required.
