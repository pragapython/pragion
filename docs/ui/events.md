# Events

The frontend runtime exposes `Pragion.events.emit(name, payload)`. Messages are JSON-shaped and are intended to cross a validated protocol boundary later. JavaScript presentation events must not contain backend business logic.
