# Design Tokens

Tokens for color, typography, spacing, radius, shadows, z-index, motion, and breakpoints are centralized in `frontend/styles/tokens.css`. Components should reference tokens rather than inventing repeated values.
