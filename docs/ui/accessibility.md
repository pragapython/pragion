# Accessibility

Pragion UI uses semantic HTML, visible focus styles, minimum touch target sizing, dialog semantics, tab roles, labels, required indicators, and reduced-motion support. Each component should preserve keyboard and screen-reader usability as it evolves.
