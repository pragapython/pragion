# Pragion UI Components

The first-party component styles are centralized in `../styles/components.css` and demonstrated in `../pages/ui-gallery.html`. Future component implementations can be split into these folders without changing the public `pragion-*` vocabulary.
